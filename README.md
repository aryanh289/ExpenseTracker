# ExpenseTracker
Python GUI based expense tracker project

Download and Install `tkinter` and `tkcalender` modules using `pip3 or pip`.
Then just run the program.
Program uses `sqlite3` module for database, if you need to install it, just do it.
